# ``Swift/String/LocalizationValue/StringInterpolation/init(literalCapacity:interpolationCount:)``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

Creates a string interpolation with presized storage for a literal with the indicated attributes.

Don’t call this method directly; the compiler uses it when interpreting string interpolations.
