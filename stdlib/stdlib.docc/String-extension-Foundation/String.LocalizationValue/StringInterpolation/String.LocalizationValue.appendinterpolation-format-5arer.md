# ``Swift/String/LocalizationValue/StringInterpolation/appendInterpolation(_:format:)-5arer``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

Appends a formatted string to a string interpolation.

Don’t call this method directly; the compiler uses it when interpreting string interpolations.

- Parameters:
  - value: A formattable value to append.
  - format: A `FormatStyle` to format `value`, prior to appending.
