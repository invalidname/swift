# ``Swift/String/LocalizationValue/StringInterpolation/appendInterpolation(_:)-8l60b``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

Appends a string that performs an out-of-process localization to a string interpolation.

Don’t call this method directly; the compiler uses it when interpreting string interpolations.

- Parameters:
  - value: The localizable string to append.
