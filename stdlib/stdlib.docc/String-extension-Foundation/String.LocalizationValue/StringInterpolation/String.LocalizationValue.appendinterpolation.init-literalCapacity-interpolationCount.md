# ``Swift/String/LocalizationValue/StringInterpolation/appendInterpolation(_:)-2cy1r``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

Appends an Objective-C object to a string interpolation.

Don’t call this method directly; the compiler uses it when interpreting string interpolations.

- Parameters:
  - subject: The Objective-C object to append.
