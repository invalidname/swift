# ``Swift/String/LocalizationValue/StringInterpolation/StringLiteralType``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

The type to use for literal segments.

The string interpolation for ``Swift/String/LocalizationValue`` uses the ``Swift/String`` type for segments.
