# ``Swift/String/LocalizationValue/StringInterpolation/appendInterpolation(placeholder:)``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

Appends a placeholder to a string interpolation.

Don’t call this method directly; the compiler uses it when interpreting string interpolations.

- Parameters:
  - placeholder: The placeholder instance to append.
