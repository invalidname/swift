# ``Swift/String/LocalizationValue/StringInterpolation/appendLiteral(_:)``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

Appends a literal segment a string interpolation.

Don’t call this method directly; the compiler uses it when interpreting string interpolations.

- Parameters:
  - literal: The literal segment to append.
