# ``Swift/String/LocalizationValue/StringInterpolation/appendInterpolation(_:)-7ud7e``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

Appends a string to a string interpolation.

Don’t call this method directly; the compiler uses it when interpreting string interpolations.

- Parameters:
  - string: The string to append.
