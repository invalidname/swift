# ``Swift/Duration/TimeFormatStyle/FormatInput``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

The type of data to format.

This format style takes ``Duration`` instances as input.
