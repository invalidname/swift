# ``Swift/Duration/UnitsFormatStyle/UnitWidth-swift.struct/wide``

@Metadata {
    @DocumentationExtension(mergeBehavior: override)
}

The full unit name.

For example, `wide` produces the unit label "3 hours" for a 3-hour duration in the `en_US` locale.
